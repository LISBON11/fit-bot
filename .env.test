# Database for Integration Tests (using 'test' schema in same DB)
DATABASE_URL=postgresql://fitbot:fitbot_dev@localhost:5432/fitbot?schema=test

# Mock API Keys (not used for real calls in mocked tests, but needed for env validation)
OPENAI_API_KEY=test_openai_key
BOT_TOKEN=test_bot_token
