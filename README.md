# FitBot 🏋️‍♀️

FitBot — это Telegram-бот для автоматического создания, парсинга и логирования тренировок. Он принимает текстовые или голосовые сообщения, конвертирует их в структурированный тренировочный план с использованием OpenAI Whisper (для перевода голоса в текст) и OpenAI GPT (NLU — извлечение сущностей) и сохраняет в базу данных.

Бот включает в себя интерактивный флоу подтверждения неоднозначных упражнений, а также функцию редактирования тренировки в любой момент времени.

## 🛠 Технологический стек

- **Язык**: TypeScript на Node.js 20 LTS
- **Telegram Framework**: [grammY](https://grammy.dev/) (с плагинами sessions и conversations)
- **База Данных**: PostgreSQL + Prisma ORM
- **Кеширование / Сессии**: Redis (`ioredis`)
- **Распознавание речи (STT)**: OpenAI Whisper API (+ ffmpeg локально)
- **NLU (Парсинг текстов)**: OpenAI GPT-4o-mini + Zod (Structured Output)
- **Логирование**: Pino
- **Развертывание**: Docker & Docker Compose
- **CI/CD**: GitHub Actions
- **Тестирование**: Jest

## 📋 Требования (Prerequisites)

Для запуска локально требуются:

- **Node.js**: v20+
- **Docker & Docker Compose**: Для запуска PostgreSQL и Redis
- **ffmpeg**: Для конвертации голосовых сообщений из `.oga` в `.wav`

На macOS ffmpeg можно установить через: `brew install ffmpeg`

## 🚀 Установка и запуск (Local Development)

1. **Клонировать репозиторий и установить зависимости**

```bash
git clone https://github.com/LISBON11/fit-bot.git
cd fit-bot
npm ci
```

2. **Настроить переменные окружения**

```bash
cp .env.example .env
# Отредактируйте .env, вставив ваши ключи от Telegram и OpenAI
```

3. **Запустить инфраструктуру (БД и Redis)**

```bash
docker compose up deps -d
# (Если в файле docker-compose.yml все сервисы объединены, можно запустить `docker compose up -d postgres redis`)
```

4. **Применить миграции и залить начальные данные (Seed)**

```bash
npx prisma migrate dev
npm run db:seed
```

5. **Запустить бота в режиме разработки**

```bash
npm run dev
```

## 🔐 Переменные окружения (.env)

| Переменная        | Описание                                   | Обязательна | По-умолчанию             |
| ----------------- | ------------------------------------------ | ----------- | ------------------------ |
| `NODE_ENV`        | Окружение (development, production)        | Нет         | `development`            |
| `LOG_LEVEL`       | Уровень логов (debug, info, warn, error)   | Нет         | `info`                   |
| `BOT_TOKEN`       | Токен Telegram-бота (от BotFather)         | Да          | -                        |
| `OPENAI_API_KEY`  | Ключ доступа к API OpenAI                  | Да          | -                        |
| `DATABASE_URL`    | Строка подключения к PostgreSQL            | Да          | -                        |
| `REDIS_URL`       | Строка подключения к Redis                 | Нет         | `redis://localhost:6379` |
| `PUBLISH_CHAT_ID` | Telegram ID чата/канала для автопубликации | Да          | -                        |

## 🕹 Команды бота

- `/start` — Начать работу и получить приветствие. Автоматически регистрирует вас в системе.
- `/help` — Получить справку по использованию бота.
- `/cancel` — Отменить текущий диалог или процесс создания/редактирования тренировки.
- `/edit` — (В разработке) Вызов меню для редактирования тренировок за определенные даты.

_Примечание: Вы также можете просто отправить любое текстовое или голосовое сообщение с вашей тренировкой, чтобы начать процесс ее парсинга._

## 🐳 Запуск через Docker (Production)

```bash
# Сборка и запуск всех сервисов (включая самого бота)
docker compose up --build -d
```
